% !TeX root = main.tex
% !TeX program = xelatex
\documentclass[11pt]{article}
\usepackage{official_preprint/styles/seed}

\title{Formal Logic as a Substrate for Symbolic Chain-of-Thought}

\author{
  Filipe Laitenberger\textsuperscript{1}, Dawid Kopiczko\textsuperscript{2}, Patrick Haller\textsuperscript{1}, Yuki Asano\textsuperscript{2}, Alan Akbik\textsuperscript{1}
}

\affiliations{\textsuperscript{1}Humboldt-Universität zu Berlin\\
\textsuperscript{2}University of Technology Nuremberg}

\preprintlogo{official_preprint/assets/hu-logo-transparent.png}
\abstractfilledfalse
\preprintdate{July 15, 2026}
\correspondence{filipe.laitenberger@hu-berlin.de}

\newcommand{\branchproof}{\textsc{BranchProof}}
\newcommand{\attrcon}{\textsc{AttrCon}}
\newcommand{\logic}{\textsc{Logic}}
\newcommand{\nl}{\textsc{Natural}}
\newcommand{\passsixteen}{pass@16}
\newif\ifshowquarantined
\showquarantinedfalse

\begin{document}

\maketitle

\begin{abstract}
Chain-of-thought supervision is usually written in natural language, even when
the latent reasoning problem is symbolic. We ask whether supervised language
models learn more reliable symbolic reasoning when the same derivation is
written instead as a compact formal trace. To isolate the trace substrate, we
construct paired synthetic tasks in which each instance has the same prompt,
answer, and latent proof, but two target traces: a formal derivation and a
natural language derivation. Attribute-constraint experiments show a positive
mean formal-trace gap in answer correctness, with substantial seed variance at
the longest depth. A post-hoc closure audit, however, found that the original
long-depth branching-proof generator reused constants and admitted multiple
derivable answers. We therefore quarantine every quantitative result based on
that construction, including its architecture, shortcut, syntax, hybrid, and
continual-pretraining derivatives. A corrected generator now assigns a fresh constant to
each layer and rejects any instance without exactly one derivable candidate;
three-seed reruns with equal generation budgets and greedy, pass@1, and pass@k
evaluation are in progress. A matched one-run Qwen2.5 continual-pretraining
pilot is null or mixed on conventional downstream suites, and its apparent
context-QA gains largely collapse after generated continuation is removed.
Until the corrected branching reruns complete, this draft provides a method,
an independent constraint-task signal, and a negative transfer pilot, not a
validated general claim that formal traces improve reasoning.
\end{abstract}

\begin{tcolorbox}[
  colback=seedblue!7,
  colframe=seedblue!45!black,
  boxrule=0.5pt,
  arc=1mm,
  left=2mm,right=2mm,top=1mm,bottom=1mm
]
\small\textbf{Internal audit status (July 15, 2026).}
All performance measurements from the original \branchproof{} construction are
excluded from the rendered preprint while corrected unique-answer experiments
run. They must not be cited or used to support the paper's claims.
Section~\ref{sec:uniqueness-audit} records the defect, its measured scope, and
the acceptance gate for replacement results.
\end{tcolorbox}

\section{Introduction}

Chain-of-thought methods improve language-model reasoning by asking the model to
produce intermediate steps before a final answer~\citep{wei2022chain,
kojima2022large}. Almost all such supervision is expressed in natural language.
That choice is useful for human readability, but it is not obviously optimal for
the model. Natural language is verbose, underspecified, and entangles the
underlying computation with discourse style. Symbolic tasks have another
natural representation: a compact formal derivation with explicit local rule
applications.

The trace language can be varied without changing the problem. Holding the
latent proof fixed, should a model be supervised to reason in natural language
or in formal logic? We focus on supervised fine-tuning rather than prompting,
because the goal is to measure what models learn from the target trace itself.
In both conditions, the model generates the trace and the answer. Validators
are used only after generation, to measure whether a produced trace is
mechanically checkable.

We use controlled synthetic tasks because the causal comparison requires paired
supervision. Each instance is sampled from a latent derivation and rendered in
two ways. The first task, \branchproof{}, contains branching Horn-style proof
chains with coherent distractor branches and controlled proof depth. The second,
\attrcon{}, contains object-attribute constraint derivations. These tasks are
not meant to be broad reasoning benchmarks. They are instruments for testing
whether a trace substrate helps models learn compositional rule application,
state tracking, distractor rejection, and extrapolation to longer derivations.

The current evidence is deliberately narrower than the motivating hypothesis.
On \attrcon{}, formal supervision has higher mean answer correctness on the
hard-depth band and at depth 50, but the endpoint has substantial seed
variance and both trace styles have weak long-depth proof validity. The
original \branchproof{} experiments appeared to provide a broader result, but
a post-hoc closure audit showed that their long-depth examples often admitted
multiple derivable answers. We therefore exclude those measurements and every
derived ablation from the evidentiary record. Corrected unique-answer reruns are
in progress with equal generation budgets and explicit greedy and pass@1
reporting.

Correctness and faithfulness are separate even when the trace syntax is formal.
A generated sequence is correct when it yields the designated answer, whereas
it is valid only when each derivation step is licensed. Formal syntax makes the
second property mechanically testable; it does not guarantee it. We therefore
report answer correctness and proof validity separately.

\textbf{Contributions and scope.} We introduce a paired experimental design
that holds prompts, answers, and latent derivations fixed while changing only
the supervised trace language. We provide an independent three-seed
attribute-constraint result that motivates the formal-substrate hypothesis, but
does not by itself establish a general advantage. We identify and measure a
long-depth uniqueness defect in the original branching benchmark, repair the
generator with fresh constants and closure-based rejection, and specify the
acceptance gate for replacement experiments. Architecture, shortcut, syntax,
hybrid, and conditioned-dual conclusions remain hypotheses until they are
rerun on the corrected construction. We additionally report a matched
one-run Qwen2.5 continual-pretraining pilot whose conventional and
context-QA results do not provide clean evidence of modality transfer.

\section{Related Work}

\subsection{Natural-language reasoning traces}

Chain-of-thought prompting showed that natural-language rationales can elicit
stronger reasoning from large models~\citep{wei2022chain}. Subsequent work
studies zero-shot reasoning prompts~\citep{kojima2022large}, rationale
bootstrapping~\citep{zelikman2022star}, decomposition into subproblems
~\citep{zhou2023least}, and search over alternative reasoning states
~\citep{yao2023tree}. These methods largely preserve natural language as the
reasoning medium. We instead vary the medium while holding the latent derivation
fixed.

Scratchpad supervision is an earlier form of the same idea: models can learn
multi-step computations more reliably when intermediate state is written into
the sequence~\citep{nye2021show}. Our question is not whether intermediate
state helps, but which representation of the same intermediate state is easiest
to learn.

\subsection{Faithfulness of reasoning traces}

Natural-language rationales need not be faithful descriptions of the computation
used by the model. Prior work shows that chain-of-thought explanations can
rationalize biased answers or omit features that influenced the prediction
~\citep{turpin2023language}, and proposes interventions for measuring CoT
faithfulness~\citep{lanham2023measuring}. Our trace-integrity controls make the
same distinction in a formal setting: a formal looking trace can support answer
accuracy without being a valid proof.

\subsection{Executable and symbolic substrates}

Program-aided language models and program-of-thought prompting use code as an
intermediate representation, often delegating computation to an interpreter
~\citep{gao2023pal,chen2023program}. Logic-based systems translate language
into formal representations and call symbolic solvers or verifiers
~\citep{pan2023logiclm,ling2023deductive}. These approaches demonstrate the
value of non-natural-language substrates, but they also change the execution
mechanism. Our experiments keep inference as plain next-token generation and
compare the supervised trace representation directly.

\subsection{Formal proof generation}

LLM-assisted theorem proving trains or prompts models to produce machine-checkable
proofs in systems such as Lean~\citep{polu2020generative,jiang2022draft,
leandojo2023,leanCopilot2024}. This literature is closest in spirit to our
formal-trace condition, but its target domain is mathematical theorem proving.
We use simpler generated tasks so that each latent derivation can be rendered as
either formal logic or natural language, enabling a controlled substrate
comparison.

\subsection{Shortcut learning}

Shortcut learning occurs when a model exploits correlations that work in the
training distribution but fail under shift~\citep{geirhos2020shortcut}. Our
generators let us create shortcut-rich training distributions while evaluating
on shortcut-neutral examples. This gives a direct test of whether a trace
language encourages rule following or brittle heuristic use.

\subsection{Synthetic compositional generalization and RLVR}

Synthetic compositional benchmarks such as SCAN and COGS are useful precisely
because they expose systematic generalization failures under controlled
distribution shifts~\citep{lake2018generalization,keysers2020measuring}. Our
tasks use the same philosophy, but the manipulated variable is the supervised
trace substrate. Recent RLVR critiques likewise emphasize that better pass@1
can reflect redistribution of existing samples rather than fundamentally new
reasoning capacity~\citep{yue2025does}. We therefore report greedy, pass@1, and
sampled pass@k separately and interpret gains as supervised
sampling/correctness effects, not as proof that the model has acquired a
general theorem-proving procedure.

\section{Method}
\label{sec:method}

\subsection{Paired reasoning tasks}

Each example is generated from a latent proof. The prompt and final answer are
identical across trace conditions. Only the target reasoning trace changes.
\branchproof{} prompts contain facts and Horn-style rules over symbolic
predicates. Exactly one branch supports the queried conclusion. Other branches
are coherent distractors. \attrcon{} prompts contain objects, slots, values, and
constraints that propagate attributes through a derivation.

\subsection{Trace substrates}

The \logic{} trace uses compact symbolic atoms and explicit rule-application
lines. The \nl{} trace describes the same derivation in short controlled
sentences. The implementation also supports symbol-padded logic, wordified
logic, pseudocode, rule-annotated natural language, shuffled traces, invalid
formal traces, same-datapoint hybrids, and mode-conditioned dual-modality
training. Results from those variants are excluded until selected controls are
rerun on the corrected benchmark.

\subsection{Evaluation protocol}

The corrected main comparison treats greedy and pass@1 final-answer correctness
as primary and reports pass@2, pass@4, pass@8, and pass@16 as secondary sampling
curves. The completed \attrcon{} experiment predates this correction and is
available at pass@16. We also report correct-plus-valid joint metrics when
validators support the trace surface. For formal traces, validity is a
mechanical proof check. For natural-language traces, validity requires
translation into the formal checker and is therefore a diagnostic rather than
a universal measure.

We use explicit depth terms throughout. The \emph{training range} contains
depths seen during fine-tuning. The
\emph{long-depth band} contains evaluated \branchproof{} depths greater than the
maximum training depth. For the main train-1-to-25 setting, this is depths
30, 35, 40, 45, and 50. The \emph{depth-50 endpoint} is the single hardest
evaluated proof depth. For \attrcon{}, we additionally report a
\emph{hard-depth band}, the fixed difficult slice used by that evaluator, and
the same depth-50 endpoint.

\section{Experimental Setup}
\label{sec:setup}

The corrected main sweep fine-tunes OLMo-3-7B on \branchproof{} with maximum
training depth in $\{5,10,15,20,25\}$ over three seeds. The principal
comparison is between paired \logic{} and \nl{} targets. Each train-depth
subset contains 50k materialized examples. We fine-tune with LoRA rank 16,
alpha 32, dropout 0.05, BF16 weights, batch size 1, learning rate $10^{-4}$,
10k optimizer steps, maximum sequence length 8192, and adapters on attention
and MLP projection modules. Architecture and mechanism ablations are deferred
until the corrected main comparison passes its artifact and sample audit.

For \attrcon{}, we use the same paired-rendering principle and report
three-seed train-1-to-25 results. This is the only completed quantitative
formal-versus-natural comparison used as evidence in the present draft. Its
depth-50 correctness and validity metrics are treated as high-variance
diagnostics, not as evidence of solved long-depth proof generation.

The corrected \branchproof{} evaluation uses depths
$\{1,2,5,10,12,15,18,20,25,30,35,40,45,50\}$, a 16,384-token context, and the
same 7,168-new-token cap for both trace languages. The final protocol evaluates
32 prompts per depth and 16 sampled generations per prompt at temperature 1.0,
with greedy, pass@1, pass@2, pass@4, pass@8, and pass@16 measurements. A
one-seed gate uses 16 prompts and eight sampled generations per depth before
the three-seed grid is released. An exhaustive audit under the evaluation
tokenizer found that all 28,000 corrected gold traces fit these limits: the
largest formal and natural-language targets contain 6,212 and 6,674 tokens,
and the largest prompt-plus-target sequences contain 13,123 and 13,596 tokens,
respectively. Thus the shared cap does not truncate either gold trace language.
\attrcon{} uses the same depth grid and 16
generations per prompt, with a family-specific hard-depth band; its existing
formal and natural evaluations use 12,288- and 8,192-token caps, respectively,
except one recovered formal row with an 8,192-token cap. All completed tables
report mean $\pm$ standard deviation across three seeds.

The continual-pretraining pilot compares one Qwen2.5-7B control run with
matched runs containing 15\% corrected unique-answer \branchproof{} tokens in
either formal or natural-language form. Each checkpoint is evaluated directly
and after the same native-Qwen-chat instruction SFT. Conventional downstream
scores use ten reviewer tasks and an audited answer-prefix MATH-500 sidecar.
The supplemental context-QA suite uses the full 200-example LongBench
HotpotQA, 2WikiMultiHopQA, and MuSiQue splits with a 32,768-token window. These
are single training runs, so their macro differences have no training-seed
uncertainty estimate.

An implementation audit found ordinary randomized mixing in the two 15\%
conditions: global updates contained 6--35 proof chunks out of 128, with a mean
of 19.2 and variance close to binomial expectation; no update was proof-empty.
All three runs shared a scheduler-resume rescaling error at the midpoint
checkpoint and a cosine span that ended above its nominal floor. These defects
alter the absolute trajectory but are matched across conditions; we corrected
both before any future continuation-training run.

\passsixteen{} is computed per prompt: a prompt is correct if any of its 16
sampled generations yields the right final answer after extracting the
\texttt{<answer>} field. Depth-level metrics average prompts within a depth,
band metrics average the relevant depths within a seed, and table entries then
average across seeds. Formal joint validity requires answer correctness and a
mechanically valid formal trace. Natural-language joint validity requires answer
correctness and successful task-specific translation of the generated natural
trace into a valid formal proof.

\section{Results}
\label{sec:results}

\subsection{Independent Attribute-Constraint Signal}

\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Trace & Hard-depth corr. & Depth-50 corr. & Hard-depth joint & Depth-50 joint \\
\midrule
\logic{} & \textbf{89.6 $\pm$ 9.0} & \textbf{45.8 $\pm$ 31.6} & 73.6 $\pm$ 5.3 & 0.0 $\pm$ 0.0 \\
\nl{} & 80.8 $\pm$ 4.9 & 8.3 $\pm$ 14.4 & \textbf{79.5 $\pm$ 3.0} & \textbf{7.3 $\pm$ 12.6} \\
\bottomrule
\end{tabular}
\caption{Three-seed \attrcon{} train-1-to-25 results. Formal supervision has
higher mean answer correctness, but the depth-50 estimate has substantial seed
variance and neither trace style reliably produces valid depth-50 proofs.}
\label{tab:attrcon-current}
\end{table}

The completed \attrcon{} comparison supports the formal-substrate hypothesis
on answer correctness, but it does not establish a general result. Formal
supervision improves mean hard-depth correctness by 8.8 percentage points and
mean depth-50 correctness by 37.5 points. The latter estimate is unstable: the
three formal seeds score 65.6\%, 9.4\%, and 62.5\%, while the natural-language
seeds score 0\%, 0\%, and 25.0\%. Moreover, natural-language supervision has
the higher joint-validity mean. We therefore treat \attrcon{} as a motivating
signal about correctness, not as evidence that formal traces uniformly improve
correctness and validity.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.84\linewidth]{official_preprint/figures/attribute_correctness.pdf}
\caption{\attrcon{} answer correctness on a distinct constraint surface. The
formal mean is higher, but the depth-50 variance prevents a strong endpoint
claim. Shaded bands are standard deviations over three seeds.}
  \label{fig:attribute-current}
\end{figure}

\subsection{The Original Branching Benchmark Is Not Valid Evidence}

Independent Horn closure shows that the original \branchproof{} construction
ceases to be a unique-answer benchmark once its 18 constants wrap. The labeled
answer remains derivable, but additional candidate answers are also derivable
in most long-depth examples (Table~\ref{tab:uniqueness-main}). This invalidates
the single-label correctness comparison rather than merely adding noise.

\begin{table}[t]
\centering
\small
\begin{tabular}{rcc}
\toprule
Depth & Multiple-answer examples & Mean derived candidates \\
\midrule
5, 10, 15 & 0/96 & 1.000 \\
20 & 73/96 & 2.562 \\
25, 30, 35 & 74/96 & 2.417 \\
40, 45, 50 & 92/96 & 3.562 \\
\bottomrule
\end{tabular}
\caption{Closure audit of the original \branchproof{} generator. Each question
has four candidate final states; every row uses 96 examples.}
\label{tab:uniqueness-main}
\end{table}

The defect also interacts with sampling. Under the invalid construction,
formal supervision was below natural-language supervision at long-depth
pass@1 but above it at pass@16. A model that samples several derivable branches
can therefore appear stronger under pass@k even when a random sample is not
more likely to match the designated label. This is why no old architecture,
shortcut, syntax, hybrid, conditioned-dual, or proof-mixture result is used as
evidence here.

\subsection{Corrected Evaluation Gate}

The repaired generator assigns a fresh constant to every layer and rejects an
instance unless closure derives exactly one candidate answer. Its production
preflight accepted all 3,000 audited examples, with maximum derived-candidate
count one. Corrected training and evaluation use materialized paired data,
equal 7,168-token generation budgets, and both greedy and pass@k metrics. A
one-seed pilot must pass an automated artifact audit and manual inspection of
train-range, held-out, and depth-50 successes and failures before the full
three-seed grid can run. Quantitative \branchproof{} claims will be added only
after that protocol completes.

\subsection{Continuation Midtraining Is Null or Mixed}

\begin{table}[t]
\centering
\small
\begin{tabular}{llccc}
\toprule
Condition & Branch & All primary & Reasoning core & Targeted logic \\
\midrule
Control & Direct & 0.6052 & 0.4946 & 0.5316 \\
Formal 15\% & Direct & 0.6085 & 0.5017 & 0.5200 \\
Natural 15\% & Direct & 0.6041 & 0.4935 & 0.5247 \\
\midrule
Control & Post-instruction & 0.4992 & 0.3051 & 0.1631 \\
Formal 15\% & Post-instruction & 0.5010 & 0.3090 & 0.1626 \\
Natural 15\% & Post-instruction & 0.5019 & 0.3147 & 0.1618 \\
\bottomrule
\end{tabular}
\caption{Corrected matched Qwen2.5-7B continual-pretraining pilot. Entries
are unweighted task macros from one run per condition, not seed means. Formal
Formal-data injection changes direct all-primary/reasoning scores by only
$+0.0033/+0.0071$ and reduces the targeted-logic macro by $0.0116$. The
post-instruction BBH cells have an extraction floor and are retained as a
diagnostic rather than modality evidence.}
\label{tab:nanotron-p15}
\end{table}

The conventional downstream comparison does not show a consistent formal or
natural-language transfer benefit (Table~\ref{tab:nanotron-p15}). Raw review
also weakens a purely capability-based reading. Direct formal and natural
mixtures continue into a generated next-document preamble more often than the
control on BBH and MMLU-Pro. Instruction SFT removes that literal marker, but
introduces long repetition and an answer-extraction floor on BBH. We therefore
do not expand the mixture grid from this result.

The corrected context-provided multi-hop suite reaches the same conclusion for
a different reason. Under stock 32-token short-answer scoring, direct
control/formal/natural mean QA-F1 is $0.189/0.250/0.238$. A sensitivity check
that removes only obvious generated continuation after the first answer span
gives $0.349/0.361/0.367$, reducing the apparent formal and natural gains to
$0.012$ and $0.018$. Under the strict tagged prompt, 98.5--99.0\% of formal
mixture rows begin the learned \texttt{<formal>} surface and 97.0--99.0\% of
natural-mixture rows begin \texttt{<think>}; most exhaust the 64-token
diagnostic before a usable answer. Post-instruction stock QA-F1 is
$0.097/0.100/0.085$. These results diagnose response-manifold and stopping
behavior, not clean multi-hop reasoning transfer.

% Historical quantitative sections are retained in source for provenance but
% are excluded from the rendered preprint after the uniqueness audit.
\ifshowquarantined
\section{Quarantined Historical Results}
\label{sec:results}

\subsection{Formal Traces Improve Length Extrapolation}

\begin{table}[t]
\centering
\small
\begin{tabular}{llcccc}
\toprule
Task and band & Trace & Band corr. & Depth-50 corr. & Band joint & Depth-50 joint \\
\midrule
\branchproof{} long-depth & \logic{} & \textbf{92.1 $\pm$ 3.4} & \textbf{83.3 $\pm$ 10.6} & 71.0 $\pm$ 9.5 & 41.7 $\pm$ 15.6 \\
\branchproof{} long-depth & \nl{} & 79.4 $\pm$ 7.7 & 51.0 $\pm$ 6.4 & \textbf{74.8 $\pm$ 5.6} & \textbf{42.7 $\pm$ 5.3} \\
\midrule
\attrcon{} hard-depth & \logic{} & \textbf{89.6 $\pm$ 9.0} & \textbf{45.8 $\pm$ 31.6} & 73.6 $\pm$ 5.3 & 0.0 $\pm$ 0.0 \\
\attrcon{} hard-depth & \nl{} & 80.8 $\pm$ 4.9 & 8.3 $\pm$ 14.4 & \textbf{79.5 $\pm$ 3.0} & \textbf{7.3 $\pm$ 12.6} \\
\bottomrule
\end{tabular}
\caption{Main train-1-to-25 three-seed results. Formal traces improve answer
correctness on both symbolic task families, especially at the depth-50 endpoint.
Joint validity is surface-specific and should not be read as the primary
cross-task metric. \branchproof{} long-depth metrics average five depths
(30 to 50) with 32 prompts per depth per seed. \attrcon{} depth-50 correctness has
high seed variance and should be treated as a replication signal, not a solved
long-depth result.}
\label{tab:main}
\end{table}

Formal traces improve the \branchproof{} depth-50 endpoint by 32.3 percentage
points. Long-depth correctness rises from 79.4\% to 92.1\%, and depth-50
correctness rises from 51.0\% to 83.3\% (Table~\ref{tab:main}). On \attrcon{},
the mean answer-correctness gap is positive but less clean. Formal traces are
stronger on the hard-depth band and much stronger at depth 50, with high
variance across seeds.
The joint-validity columns show a different fact. Natural language slightly
exceeds formal traces on \branchproof{} depth-50 joint validity, and \attrcon{}
depth-50 validity remains weak for both trace styles. The robust claim is
therefore answer correctness, not automatic proof faithfulness. For
\attrcon{}, the formal depth-50 advantage is large in the mean but noisy across
seeds: the three formal seeds score 65.6\%, 9.4\%, and 62.5\% depth-50
correctness, while natural-language seeds score 0\%, 0\%, and 25.0\%.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.93\linewidth]{official_preprint/figures/main_correctness.pdf}
\caption{\branchproof{} correctness as the maximum training depth increases.
The left panel averages evaluated depths beyond the training range. The right
panel isolates the depth-50 endpoint. Formal traces are strongest when the
required derivation is longer than those seen in training. Shaded bands are
standard deviations over three seeds.}
  \label{fig:main-correctness}
\end{figure}

The depth curve localizes the gain to extrapolation. Performance is not
monotonic at small training ranges, but the strongest formal models
extrapolate better than the strongest natural-language models
(Figure~\ref{fig:main-correctness}).

\begin{figure}[t]
  \centering
  \includegraphics[width=0.84\linewidth]{official_preprint/figures/attribute_correctness.pdf}
\caption{\attrcon{} is a high-variance replication check on a different
symbolic surface. Formal traces have the higher mean answer correctness, but
the large depth-50 variance and weak depth-50 joint validity make this a
caveat figure rather than a clean second solved task. Shaded bands are standard
deviations over three seeds.}
  \label{fig:attribute-correctness}
\end{figure}

\subsection{Formal Traces Resist Shortcut Shift}

Shortcut-rich training exposes a larger natural-language failure mode. During
training, shortcut markers identify the active branch with probability $r$. At
evaluation, examples are shortcut-neutral.

\begin{table}[t]
\centering
\scriptsize
\begin{tabular}{lcccc}
\toprule
Trace & Shortcut rate & Long-depth corr. & Long-depth joint & Depth-50 corr. \\
\midrule
\logic{} & 0.0 & 92.1 $\pm$ 3.4 & 71.0 $\pm$ 9.5 & 83.3 $\pm$ 10.6 \\
\logic{} & 0.3 & 89.2 $\pm$ 1.0 & 59.8 $\pm$ 3.1 & 84.4 $\pm$ 0.0 \\
\logic{} & 0.5 & 90.6 $\pm$ 1.2 & 67.7 $\pm$ 3.8 & 83.3 $\pm$ 4.8 \\
\logic{} & 0.8 & 94.0 $\pm$ 3.2 & 79.4 $\pm$ 4.5 & 82.3 $\pm$ 13.0 \\
\midrule
\nl{} & 0.0 & 79.4 $\pm$ 7.7 & 74.8 $\pm$ 5.6 & 51.0 $\pm$ 6.4 \\
\nl{} & 0.3 & 58.8 $\pm$ 23.8 & 57.1 $\pm$ 25.7 & 45.8 $\pm$ 36.5 \\
\nl{} & 0.5 & 64.2 $\pm$ 27.2 & 58.5 $\pm$ 28.8 & 38.5 $\pm$ 36.2 \\
\nl{} & 0.8 & 63.8 $\pm$ 3.8 & 56.5 $\pm$ 1.0 & 28.1 $\pm$ 6.2 \\
\bottomrule
\end{tabular}
\caption{Shortcut-rate ablation on \branchproof{}. Evaluation removes the
shortcut marker. Formal depth-50 correctness stays near 82 to 84\%, while
natural-language depth-50 correctness deteriorates as shortcuts become common.
Rows are mean $\pm$ standard deviation over three seeds.}
\label{tab:shortcut-rate}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.58\linewidth]{official_preprint/figures/shortcut_robustness.pdf}
\caption{Depth-50 correctness under shortcut-rich training. As shortcut
markers become more common during training, natural-language traces become
less reliable on shortcut-neutral depth-50 evaluation, while formal traces
remain comparatively stable. Shaded bands are standard deviations over three
seeds.}
  \label{fig:shortcut}
\end{figure}

The formal advantage is not only a clean-distribution effect. Natural-language
traces can produce correct answers in normal training, but this shortcut schema
exposes greater fragility at long proof depths (Table~\ref{tab:shortcut-rate}
and Figure~\ref{fig:shortcut}). Appendix~\ref{sec:shortcut-kind} reports
additional shortcut mechanisms, including one natural-language initial-marker
exception. The supported claim is schema-level sensitivity, not that every
shortcut construction hurts natural language.

\subsection{The Pattern Repeats Across Model Families}

\begin{table}[t]
\centering
\small
\begin{tabular}{llcc}
\toprule
Model & Trace & Long-depth corr. & Depth-50 corr. \\
\midrule
OLMo-7B & \logic{} & \textbf{92.1 $\pm$ 3.4} & \textbf{83.3 $\pm$ 10.6} \\
OLMo-7B & \nl{} & 79.4 $\pm$ 7.7 & 51.0 $\pm$ 6.4 \\
\midrule
Qwen-2.5-1.5B & \logic{} & \textbf{77.1 $\pm$ 18.2} & \textbf{70.8 $\pm$ 34.6} \\
Qwen-2.5-1.5B & \nl{} & 54.2 $\pm$ 3.3 & 43.8 $\pm$ 8.8 \\
\midrule
Qwen-2.5-7B & \logic{} & \textbf{90.6 $\pm$ 3.6} & \textbf{85.4 $\pm$ 6.4} \\
Qwen-2.5-7B & \nl{} & 56.9 $\pm$ 9.0 & 25.0 $\pm$ 22.7 \\
\midrule
Gemma-3-4B & \logic{} & \textbf{69.6 $\pm$ 5.5} & \textbf{56.2 $\pm$ 11.1} \\
Gemma-3-4B & \nl{} & 39.4 $\pm$ 34.6 & 30.2 $\pm$ 42.7 \\
\bottomrule
\end{tabular}
\caption{\branchproof{} architecture check at train depth 25. Formal traces are
directionally better across the tested model families, but several rows have
large seed variance. This is a consistency check, not a scaling law.}
\label{tab:architecture}
\end{table}

The formal direction repeats across the tested model families
(Table~\ref{tab:architecture}). The variance is large for some smaller or less
stable runs, especially Gemma-3-4B and Qwen-2.5-1.5B, so this result should not
be over-read as a scaling law. It does show that the \branchproof{} effect is
not unique to one checkpoint family.

\section{Analysis}
\label{sec:analysis}

\subsection{What the Ablations Rule Out}

The ablations are organized around specific objections. Is the effect just more
token exposure? Is it code-like formatting? Is it explicit rule annotation? Is
it simply that formal traces are longer or more structured? The controls do not
support those simple explanations, but they also do not isolate a single causal
feature. They narrow the explanation to a cluster of substrate properties:
compact symbols, local rule applications, low discourse load, and mechanical
grounding.

\begin{table}[t]
\centering
\scriptsize
\begin{tabular}{p{0.24\linewidth}p{0.37\linewidth}cc}
\toprule
Training trace & Explanation tested & Long-depth corr. & Depth-50 corr. \\
\midrule
Compact formal & Main formal substrate & \textbf{92.1 $\pm$ 3.4} & \textbf{83.3 $\pm$ 10.6} \\
Natural language & Main natural-language substrate & 79.4 $\pm$ 7.7 & 51.0 $\pm$ 6.4 \\
Same-token formal & More target-token exposure is not the cause & 89.8 $\pm$ 6.6 & 79.2 $\pm$ 11.0 \\
Same-token natural & Equalizing token budget does not rescue NL & 55.4 $\pm$ 10.6 & 34.4 $\pm$ 16.5 \\
Symbol-padded formal & Longer formal syntax is not enough & 67.5 $\pm$ 11.6 & 56.2 $\pm$ 10.8 \\
Wordified formal & Replacing compact symbols with word-like predicates hurts & 50.8 $\pm$ 11.6 & 34.4 $\pm$ 10.8 \\
Pseudocode & Code-like formatting is not enough & 54.4 $\pm$ 24.8 & 20.8 $\pm$ 10.0 \\
Rule-annotated natural & Explicit rule labels are not enough & 57.5 $\pm$ 5.4 & 34.4 $\pm$ 19.0 \\
\bottomrule
\end{tabular}
\caption{Surface and token-budget controls at train depth 25. The gain is not
recovered by the tested length, rule-label, wordification, or code-like-format
controls. Rows are mean $\pm$ standard deviation over three seeds.}
\label{tab:syntax}
\end{table}

The simple explanations fail in the controls. The formal model does not win
merely because it sees more useful target tokens. Same-token formal nearly
preserves answer correctness but loses much of its validity
(Table~\ref{tab:syntax}). The gain is also not recovered by making logic
longer, replacing symbols with words, adding rule labels to natural language,
or wrapping steps in pseudocode. We omit the ``terse natural'' row from the main
table because the artifact audit found it is not a clean shorter-NL control. In
the inspected training sequence it is token-identical to the natural-language
baseline.

\subsection{Correct Answers Are Not Valid Proofs}

\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Trace condition & Long-depth corr. & Long-depth formal joint & Depth-50 corr. & Depth-50 formal joint \\
\midrule
Compact formal & 92.1 $\pm$ 3.4 & \textbf{71.0 $\pm$ 9.5} & 83.3 $\pm$ 10.6 & \textbf{41.7 $\pm$ 15.6} \\
Invalid formal & 89.2 $\pm$ 5.6 & 0.0 $\pm$ 0.0 & 75.0 $\pm$ 11.3 & 0.0 $\pm$ 0.0 \\
Shuffled formal & 69.0 $\pm$ 2.6 & 0.0 $\pm$ 0.0 & 51.0 $\pm$ 10.0 & 0.0 $\pm$ 0.0 \\
\bottomrule
\end{tabular}
\caption{Trace integrity controls. Invalid formal looking traces can preserve
high answer correctness while mechanical proof validity collapses, so formal
syntax alone is not a faithfulness guarantee.}
\label{tab:integrity}
\end{table}

Trace corruption exposes the faithfulness boundary. The model can exploit
formal looking regularities and still answer correctly even when the trace is
not a valid proof (Table~\ref{tab:integrity}). This does not undo the
correctness results, but it means that proof faithfulness requires additional
training objectives, decoding constraints, or verification.

\subsection{Combining Substrates Is Capacity-Dependent}

\begin{table}[t]
\centering
\small
\begin{tabular}{lcc}
\toprule
Training format & Long-depth corr. & Depth-50 corr. \\
\midrule
Formal only & \textbf{92.1 $\pm$ 3.4} & \textbf{83.3 $\pm$ 10.6} \\
Natural only & 79.4 $\pm$ 7.7 & 51.0 $\pm$ 6.4 \\
Formal then natural, same target & 87.9 $\pm$ 4.5 & 64.6 $\pm$ 18.8 \\
Natural then formal, same target & 57.3 $\pm$ 4.5 & 34.4 $\pm$ 6.2 \\
Conditioned dual, formal prompt & 83.3 $\pm$ 13.5 & 67.7 $\pm$ 25.5 \\
Conditioned dual, natural prompt & 67.5 $\pm$ 8.2 & 53.1 $\pm$ 6.2 \\
\bottomrule
\end{tabular}
\caption{OLMo-7B hybrid and conditioned-dual results at train depth 25.
Same-target hybrids are order-sensitive, and conditioned-dual training
underperforms the matched single-modality formal run at this model size.}
\label{tab:hybrid}
\end{table}

Hybrid traces do not recover the formal-only baseline at OLMo-7B. Formal-first
hybrids are much better than natural-language-first hybrids, indicating that
order matters, but they still fall short of formal-only training on depth-50
correctness (Table~\ref{tab:hybrid}). Separate mode-conditioned examples
remove the same-target length issue and train longer, but still do not recover
the formal-only baseline at 7B.

\begin{table}[t]
\centering
\scriptsize
\begin{tabular}{llccc}
\toprule
Model & Mode & Single modality & Conditioned dual & $\Delta$ dual \\
\midrule
OLMo-7B & formal prompt, long-depth corr. & 92.1 $\pm$ 3.4 & 83.3 $\pm$ 13.5 & -8.8 \\
OLMo-7B & natural prompt, long-depth corr. & 79.4 $\pm$ 7.7 & 67.5 $\pm$ 8.2 & -11.9 \\
\midrule
OLMo-3-32B & formal prompt, long-depth corr. & 95.4 $\pm$ 2.8 & \textbf{96.3 $\pm$ 3.3} & +0.8 \\
OLMo-3-32B & natural prompt, long-depth corr. & \textbf{68.5 $\pm$ 4.6} & 60.8 $\pm$ 24.4 & -7.7 \\
OLMo-3-32B & formal prompt, hard-tail corr. & 97.5 $\pm$ 1.6 & \textbf{97.9 $\pm$ 1.8} & +0.5 \\
OLMo-3-32B & natural prompt, hard-tail corr. & \textbf{82.5 $\pm$ 2.6} & 78.2 $\pm$ 13.5 & -4.3 \\
\bottomrule
\end{tabular}
\caption{Capacity follow-up for conditioned-dual supervision. The 7B rows show
the original negative result. At OLMo-3-32B, the formal conditioned mode closes
and slightly reverses the single-modality gap on answer correctness, while the
natural-language conditioned mode remains below its single-modality baseline.
The 32B hard-tail rows use the evaluator's hard-tail band; comparisons are
within model and mode. Conditioned-dual runs use the same optimizer-step budget
as the single-modality runs, so this is a mixed-exposure comparison rather than
an additive-data control.}
\label{tab:conditioned-capacity}
\end{table}

The OLMo-3-32B follow-up changes the interpretation of conditioned dual
training. The 7B result suggests interference or insufficient capacity, rather
than a fundamental mismatch in the conditioned prompts. When the same idea is
tested with OLMo-3-32B, conditioned formal reasoning matches or slightly exceeds
the single-modality formal baseline on long-depth answer correctness
(Table~\ref{tab:conditioned-capacity}). The effect is mode-specific. The
natural-language conditioned mode has much higher seed variance and still
trails single-modality natural-language training on the mean. A matched sample
audit found no mode leakage: conditioned formal generations stayed in the
\texttt{<formal>} format, conditioned natural-language generations stayed in
\texttt{<think>}, and the relevant failures were wrong branch or state
continuations rather than prompt-format collapse. We therefore read conditioned
dual supervision as a capacity-dependent way to let the model use formal
reasoning without requiring that every generated chain of thought be purely
formal, not as evidence that mixed-mode training improves both modes.

\begin{figure}[t]
  \centering
  \begin{tabular}{cc}
    \includegraphics[width=0.48\linewidth]{official_preprint/figures/hybrid_order.pdf} &
    \includegraphics[width=0.42\linewidth]{official_preprint/figures/conditioned_dual.pdf}
  \end{tabular}
\caption{OLMo-7B combinations of the two trace substrates. Same-target hybrids
are order-sensitive, and the 7B mode-conditioned runs trail the corresponding
single-modality baselines; Table~\ref{tab:conditioned-capacity} shows that the
formal conditioned mode catches up at 32B.
Shaded bands are standard deviations over three seeds.}
  \label{fig:hybrids}
\end{figure}

\fi

\section{Discussion}
\label{sec:discussion}

The completed \attrcon{} experiment makes formal supervision a credible
hypothesis for improving symbolic answer correctness, but the evidence is not
yet broad enough for a general substrate claim. Its mean gap is positive, its
hardest endpoint is noisy, and its joint-validity result does not favor formal
traces. The corrected \branchproof{} experiment is designed to determine
whether the correctness effect replicates under controlled length
extrapolation once every question has exactly one derivable answer.

The uniqueness failure illustrates why the distinction between correctness,
validity, and sampling budget is substantive. Pass@k can reward coverage of
several derivable branches even when pass@1 does not improve. A mechanically
valid trace can also disagree with a single designated label when the benchmark
is ambiguous. We therefore require unique-answer closure, greedy and pass@1
results, equal generation budgets, and raw-generation inspection before using
pass@k as evidence.

The old mechanism and mixed-modality experiments remain useful for choosing
follow-ups, but not for supporting claims. If the corrected three-seed result
shows a formal advantage at greedy or pass@1, the next experiments should
selectively rerun one syntax control, one shortcut intervention, one
conditioned-dual comparison, and one independent architecture. If it does not,
the appropriate conclusion is that the original broad narrative was an
artifact of benchmark ambiguity rather than evidence for a better substrate.

The corrected continual-pretraining pilot provides a separate negative
constraint. Small aggregate differences, next-document continuation, tagged
trace-surface triggering, and cap-sensitive context-QA scores make it unsafe to
claim downstream modality transfer from the current one-run comparison. This
does not determine the supervised \branchproof{} result, but it rejects the
planned broader mixture expansion under the positive-and-clean gate.

\section{Limitations}

The tasks are synthetic. This is necessary for a paired substrate comparison,
but it limits claims about open-domain reasoning. The only completed three-seed
comparison currently retained as positive evidence is \attrcon{}, whose
depth-50 estimate has high seed variance. Corrected \branchproof{} results are
still pending. The
completed continual-pretraining comparison has one run per condition, small mixed deltas,
and response-format artifacts, so it cannot estimate seed variance or establish
a transfer effect. Natural-language validity depends on task-specific
translation coverage, while formal validity does not imply causal faithfulness.
Other formal languages, proof granularities, and decoding constraints may
change the tradeoffs.

\section{Conclusion}

Paired trace supervision provides a direct way to test whether formal logic is
a better training substrate than natural language. The current
attribute-constraint result motivates that hypothesis, while the uniqueness
audit invalidates the original branching-proof evidence and its derived
ablations. A corrected continual-pretraining pilot is null or mixed and
shows that response control can masquerade as context-QA transfer. The decisive
supervised claim therefore remains open pending the corrected unique-answer
rerun. Regardless of its outcome, correctness and proof validity must be
evaluated separately.

\clearpage
\appendix

\section{Post-hoc Branching-Proof Uniqueness Audit}
\label{sec:uniqueness-audit}

The original generator selected from 18 one-letter constants and wrapped the
constant sequence after depth 17. Reusing state predicates at reused constants
allowed distinct branches to derive the same atoms. Independent Horn closure
over 96 examples per depth found the labeled answer derivable in every case,
but found additional derivable candidate answers in most long-depth cases
(Table~\ref{tab:uniqueness-audit}).

\begin{table}[h]
\centering
\small
\begin{tabular}{rcc}
\toprule
Depth & Examples with multiple answers & Mean derived candidates \\
\midrule
5, 10, 15 & 0/96 & 1.000 \\
20 & 73/96 & 2.562 \\
25, 30, 35 & 74/96 & 2.417 \\
40, 45, 50 & 92/96 & 3.562 \\
\bottomrule
\end{tabular}
\caption{Closure audit of the quarantined branching-proof construction. Each
question has four candidate final states.}
\label{tab:uniqueness-audit}
\end{table}

This defect also changes the interpretation of the old sample-budget curves.
For the OLMo-7B train-1-to-25 runs, mean long-depth pass@1 was $0.513$ for
formal traces and $0.574$ for natural-language traces; pass@16 was $0.921$ and
$0.794$, respectively. At depth 50, pass@1 was $0.241$ versus $0.147$, while
pass@16 was $0.833$ versus $0.510$. One stored formal generation answered a
non-labeled candidate that the citation-free proof checker accepted as valid,
directly confirming that the single-label correctness metric penalized a valid
answer under the old premises.

The corrected construction uses fresh constants \texttt{c0} through
\texttt{c\{depth\}} and explicit atoms such as \texttt{A(c18)}. Its production
preflight checked 1,000 train and 2,000 evaluation instances, with unique-answer
rate $1.0$, maximum derived-candidate count 1, no generation failures, and
balanced answer positions. Replacement evaluation uses a 16,384-token context,
the same 7,168-new-token cap for both trace languages, greedy decoding, and
pass@k for $k\in\{1,2,4,8,16\}$. Across every corrected validation row and
depth, the minimum measured headroom is 494 tokens for generation and 2,788
tokens for the full context. Quantitative claims will be restored only
after raw generations and validators pass a one-seed pilot and the full
three-seed rerun completes.

\section{Task Details}
\label{sec:task-details}

\branchproof{} samples a target proof path of controlled depth and adds
distractor branches that are locally coherent but do not support the queried
answer. Shortcut variants add training-only marker correlations that identify
the active branch with a chosen probability. Evaluation removes those markers.

\attrcon{} samples objects, slots, values, and propagation constraints. The
formal trace names slot/value relations directly. The natural trace describes
the same propagation in controlled prose. This changes the symbolic surface
while preserving the paired-rendering principle.

\section{Training and Evaluation Details}
\label{sec:training-details}

All main SFT runs use materialized datasets so that the formal and natural
conditions see the same prompts and answers. Each train-depth subset contains
50k examples, and the online validation subset contains 256 examples. Main runs
train for 10k optimizer steps with LoRA adapters on \texttt{q\_proj},
\texttt{k\_proj}, \texttt{v\_proj}, \texttt{o\_proj}, \texttt{up\_proj},
\texttt{down\_proj}, and \texttt{gate\_proj}. The LoRA rank is 16, alpha is 32,
dropout is 0.05, the optimizer learning rate is $10^{-4}$, the per-device batch
size is 1, gradient accumulation is 1 unless an ablation states otherwise, and
training uses BF16 with gradient checkpointing.

Post-hoc evaluation uses vLLM with top-$p=0.95$, temperature 1.0, 16 sampled
generations per prompt, and pass@k values $\{1,2,4,8,16\}$. The explicit depth
grid is $\{1,2,5,10,12,15,18,20,25,30,35,40,45,50\}$. For each seed and depth
we evaluate 32 prompts, so a train-1-to-25 \branchproof{} long-depth band
contains $5 \times 32=160$ prompts per seed. Reported uncertainty is seed-level
standard deviation, not a confidence interval.

\section{Trace Grammar and Validation}
\label{sec:grammar}

Formal traces contain four blocks: constants, predicates, premises, and proof
lines, followed by a conclusion and answer. A proof line contains a formula and
a rule justification such as premise retrieval or implication elimination. The
formal checker parses the generated blocks and verifies that each proof line is
licensed by earlier lines and premises. Natural-language traces use controlled
sentences for the same premises and proof steps. When possible, a
task-specific translator maps those sentences back into the formal checker.
Because translation coverage is task-specific, final-answer correctness is the
primary cross-task metric and translated validity is reported as a diagnostic.

The operational \branchproof{} formal surface is:

\begin{verbatim}
<formal>
<constants> CONST_DEF+ </constants>
<predicates> PRED_DEF+ </predicates>
<premises> FORMULA+ </premises>
<proof> PROOF_LINE+ </proof>
<conclusion> ATOM </conclusion>
<answer> LABEL </answer>

CONST_DEF  ::= CONST "=" LABEL
PRED_DEF   ::= PRED "x:" "x is" LABEL
ATOM       ::= PRED CONST
FORMULA    ::= ATOM | ATOM "&" ATOM "->" ATOM | ATOM "->" ATOM
PROOF_LINE ::= FORMULA "; R," REF
             | ATOM "; ->E," REF "," REF
             | ATOM "&" ATOM "; &I," REF "," REF
\end{verbatim}

The \attrcon{} surface uses the same block structure but replaces unary
predicate atoms with object-slot-value relations. In both tasks, references
must point to earlier proof lines or listed premises. Generated lines cannot
justify future steps.

\section{Generator and Evaluation Mechanics}
\label{sec:generator-eval}

The \branchproof{} generator samples a target path, assigns labels to constants
and predicates, then adds distractor branches that remain locally coherent but
do not derive the queried conclusion. In pseudocode:

\begin{verbatim}
sample target answer label
sample initial object state
for depth step in 1..D:
    sample active branch rule(s)
    derive next object state
    add coherent distractor rules for inactive branches
render one prompt
render the same latent proof as formal and natural-language targets
\end{verbatim}

The \attrcon{} generator follows the same paired-rendering principle with
objects, slots, values, and propagation constraints. The natural-language target
is not free-form prose. It is controlled text produced from the same latent
derivation.

Answer extraction first searches for the final \texttt{<answer>} field. If no
well-formed final answer is present, the generation is counted incorrect even
when earlier text contains the gold label. Formal validation then parses block
boundaries, declarations, formulas, proof-line references, and rule
applications. Common validation failures are malformed block structure,
undeclared symbols, references to unavailable lines, applying implication
elimination to the wrong antecedent, and stopping before the queried conclusion.
Natural-language validation adds one more possible failure mode: the controlled
sentence must translate back into a formal step before the proof checker can
evaluate it.

\section{Paired Target Examples}
\label{sec:paired-example}

The following minimal examples show the paired target format used throughout the
experiments. The prompt and answer are identical within each pair. Only the
target trace substrate changes. Full experimental examples include more
distractor premises and longer derivations.

\paragraph{\branchproof{} example 1.}

\begin{verbatim}
Prompt:
a is ruby. a is north.
If a is north and a is ruby, then b is olive.
What is b?
\end{verbatim}

\noindent\begin{minipage}[t]{0.48\linewidth}
\textbf{Formal target}
\scriptsize
\begin{verbatim}
<formal>
<constants> a = a b = b </constants>
<predicates> Rx: x is ruby Nx: x is north
Ox: x is olive </predicates>
<premises> Ra Na Na & Ra -> Ob </premises>
<proof> Ra ; R,1
Na ; R,2
Na & Ra ; &I,5,4
Ob ; ->E,3,6 </proof>
<conclusion> Ob </conclusion>
<answer> olive </answer>
\end{verbatim}
\end{minipage}\hfill
\begin{minipage}[t]{0.48\linewidth}
\textbf{Natural-language target}
\scriptsize
\begin{verbatim}
<think>
a is ruby. a is north.
Since a is north and a is ruby, b is olive.
Therefore, b is olive.
</think>
<answer> olive </answer>
\end{verbatim}
\end{minipage}

\paragraph{\branchproof{} example 2.}

\begin{verbatim}
Prompt:
a is cobalt. a is east.
If a is east and a is cobalt, then b is maple.
If b is maple, then b is south.
If b is south and b is maple, then c is cedar.
What is c?
\end{verbatim}

\noindent\begin{minipage}[t]{0.48\linewidth}
\textbf{Formal target}
\scriptsize
\begin{verbatim}
<formal>
<constants> a = a b = b c = c </constants>
<predicates> Cx: x is cobalt Ex: x is east
Mx: x is maple Sx: x is south Dx: x is cedar </predicates>
<premises> Ca Ea
Ea & Ca -> Mb
Mb -> Sb
Sb & Mb -> Dc </premises>
<proof> Ca ; R,1
Ea ; R,2
Ea & Ca ; &I,7,6
Mb ; ->E,3,8
Sb ; ->E,4,9
Sb & Mb ; &I,10,9
Dc ; ->E,5,11 </proof>
<conclusion> Dc </conclusion>
<answer> cedar </answer>
\end{verbatim}
\end{minipage}\hfill
\begin{minipage}[t]{0.48\linewidth}
\textbf{Natural-language target}
\scriptsize
\begin{verbatim}
<think>
a is cobalt. a is east.
Since a is east and a is cobalt, b is maple.
Since b is maple, b is south.
Since b is south and b is maple, c is cedar.
Therefore, c is cedar.
</think>
<answer> cedar </answer>
\end{verbatim}
\end{minipage}

\paragraph{\attrcon{} example 1.}

\begin{verbatim}
Prompt:
s0 has value red. s1 has value blue.
If s0 is red and s1 is blue, then s2 is green.
Which values fill all constrained slots?
\end{verbatim}

\noindent\begin{minipage}[t]{0.48\linewidth}
\textbf{Formal target}
\scriptsize
\begin{verbatim}
<formal>
<constants> s0 = attribute slot 0 s1 = attribute slot 1
s2 = attribute slot 2
red = attribute value red
blue = attribute value blue
green = attribute value green </constants>
<predicates> Value(x,y): slot x has value y
Constraint(x,y,z,w,u,v): values y and w at slots x and z
jointly force value v at slot u </predicates>
<premises> Value(s0,red) Value(s1,blue)
Constraint(s0,red,s1,blue,s2,green)
Value(s0,red) & Value(s1,blue) &
Constraint(s0,red,s1,blue,s2,green)
-> Value(s2,green) </premises>
<proof> Value(s0,red) ; R,1
Value(s1,blue) ; R,2
Constraint(s0,red,s1,blue,s2,green) ; R,3
Value(s0,red) & Value(s1,blue) ; &I,5,6
Value(s0,red) & Value(s1,blue) &
Constraint(s0,red,s1,blue,s2,green) ; &I,8,7
Value(s2,green) ; ->E,4,9 </proof>
<answer> red-blue-green </answer>
\end{verbatim}
\end{minipage}\hfill
\begin{minipage}[t]{0.48\linewidth}
\textbf{Natural-language target}
\scriptsize
\begin{verbatim}
<think>
s0 has value red.
s1 has value blue.
The applicable joint constraint maps s0=red and s1=blue
to s2=green.
Combine the two prerequisite slot values.
Therefore s2 has value green.
</think>
<answer> red-blue-green </answer>
\end{verbatim}
\end{minipage}

\paragraph{\attrcon{} example 2.}

\begin{verbatim}
Prompt:
s0 has value amber. s1 has value ivory.
If s0 is amber and s1 is ivory, then s2 is olive.
If s0 is amber and s2 is olive, then s3 is coral.
Which values fill all constrained slots?
\end{verbatim}

\noindent\begin{minipage}[t]{0.48\linewidth}
\textbf{Formal target}
\scriptsize
\begin{verbatim}
<formal>
<constants> s0 = attribute slot 0 s1 = attribute slot 1
s2 = attribute slot 2 s3 = attribute slot 3
amber = attribute value amber
ivory = attribute value ivory
olive = attribute value olive
coral = attribute value coral </constants>
<predicates> Value(x,y): slot x has value y
Constraint(x,y,z,w,u,v): values y and w at slots x and z
jointly force value v at slot u </predicates>
<premises> Value(s0,amber) Value(s1,ivory)
Constraint(s0,amber,s1,ivory,s2,olive)
Value(s0,amber) & Value(s1,ivory) &
Constraint(s0,amber,s1,ivory,s2,olive) -> Value(s2,olive)
Constraint(s0,amber,s2,olive,s3,coral)
Value(s0,amber) & Value(s2,olive) &
Constraint(s0,amber,s2,olive,s3,coral)
-> Value(s3,coral) </premises>
<proof> Value(s0,amber) ; R,1
Value(s1,ivory) ; R,2
Constraint(s0,amber,s1,ivory,s2,olive) ; R,3
Value(s0,amber) & Value(s1,ivory) ; &I,7,8
Value(s0,amber) & Value(s1,ivory) &
Constraint(s0,amber,s1,ivory,s2,olive) ; &I,10,9
Value(s2,olive) ; ->E,4,11
Constraint(s0,amber,s2,olive,s3,coral) ; R,5
Value(s0,amber) & Value(s2,olive) ; &I,7,12
Value(s0,amber) & Value(s2,olive) &
Constraint(s0,amber,s2,olive,s3,coral) ; &I,14,13
Value(s3,coral) ; ->E,6,15 </proof>
<answer> amber-ivory-olive-coral </answer>
\end{verbatim}
\end{minipage}\hfill
\begin{minipage}[t]{0.48\linewidth}
\textbf{Natural-language target}
\scriptsize
\begin{verbatim}
<think>
s0 has value amber.
s1 has value ivory.
The applicable joint constraint maps s0=amber and s1=ivory
to s2=olive.
Combine the two prerequisite slot values.
Therefore s2 has value olive.
The applicable joint constraint maps s0=amber and s2=olive
to s3=coral.
Combine the two prerequisite slot values.
Therefore s3 has value coral.
</think>
<answer> amber-ivory-olive-coral </answer>
\end{verbatim}
\end{minipage}

\ifshowquarantined
\section{Artifact Control Not Used as Main Evidence}
\label{sec:excluded-controls}

The terse-NL artifact is not used as a main mechanism result. The audit found
that, for the inspected training sequence, the current terse-NL target is
token-identical to the natural-language baseline, so it is not a clean shorter
natural-language control. Its result remains in the generated CSV artifacts but
is omitted from the main syntax table and figure.

\section{Additional Shortcut Variants}
\label{sec:shortcut-kind}

\begin{table}[h]
\centering
\scriptsize
\begin{tabular}{llccc}
\toprule
Shortcut type & Trace & Rate & Long-depth corr. & Depth-50 corr. \\
\midrule
Position & \logic{} & 0.5 & 90.0 $\pm$ 6.0 & 84.4 $\pm$ 5.4 \\
Position & \logic{} & 0.8 & 87.9 $\pm$ 2.4 & 76.0 $\pm$ 1.8 \\
Position & \nl{} & 0.5 & 54.0 $\pm$ 17.3 & 39.6 $\pm$ 9.0 \\
Position & \nl{} & 0.8 & 51.3 $\pm$ 25.0 & 39.6 $\pm$ 33.1 \\
\midrule
Initial marker & \logic{} & 0.5 & 88.3 $\pm$ 2.5 & 85.4 $\pm$ 9.5 \\
Initial marker & \logic{} & 0.8 & 88.5 $\pm$ 4.2 & 86.5 $\pm$ 9.0 \\
Initial marker & \nl{} & 0.5 & 46.9 $\pm$ 12.0 & 11.5 $\pm$ 12.6 \\
Initial marker & \nl{} & 0.8 & 77.1 $\pm$ 20.4 & 66.7 $\pm$ 13.0 \\
\bottomrule
\end{tabular}
\caption{Additional shortcut mechanisms. Formal traces are consistently robust.
The natural-language initial-marker rate-0.8 row is a measured exception and
should be interpreted as distribution-specific rather than a general shortcut
benefit. Rows are mean $\pm$ standard deviation over three seeds.}
\label{tab:shortcut-kind}
\end{table}
\fi

\section{Metric Definitions}

Answer correctness is computed after extracting the final answer field from the
generation. Formal joint validity requires both answer correctness and a
mechanically valid formal trace. Natural-language joint validity requires answer
correctness and successful translation of the generated natural-language trace
into a valid formal proof. Because the natural-language translator is
task-specific, answer correctness is the primary cross-task metric.

\bibliographystyle{plainnat}
\bibliography{official_preprint/references}

\end{document}
